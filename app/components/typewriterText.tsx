"use client"

import Typewriter from 'typewriter-effect'

export default function TypewriterText({options}: {options: Object}){
    return (
        <Typewriter
            options={options}
        />
    )
}